[build]
  command = "npm run build"
  publish = "dist"

# Forward all /api requests to the live Render backend
[[redirects]]
  from = "/api/*"
  to = "https://risecredit-api.onrender.com/api/:splat"
  status = 200
  force = true

# Handle React Router (SPA) - all other routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
